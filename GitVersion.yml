mode: ContinuousDelivery
next-version: 0.1.0

branches:
  main:
    regex: ^main$
    increment: Minor
    tag: ""

  feature:
    regex: ^feature[/-]
    increment: Inherit
    source-branches: [ main ]
    tag: useBranchName
